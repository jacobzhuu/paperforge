[project]
name = "paperforge-mcp-runtime"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["mcp==2.2.0", "httpx2>=2.5,<3", "jsonschema>=4.20,<5"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["mcp_runtime"]
