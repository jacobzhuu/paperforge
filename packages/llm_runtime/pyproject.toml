[project]
name = "llm-runtime"
version = "0.1.0"
description = "PaperForge LLM provider seam、角色路由、JSON 净化"
requires-python = ">=3.12"
dependencies = ["httpx>=0.27"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["llm_runtime"]
