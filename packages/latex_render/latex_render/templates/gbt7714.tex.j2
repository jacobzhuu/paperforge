%% PaperForge 生成的 LaTeX 工程（模板：中文学报 / GB-T 7714 参考文献）
%% 导言区由模板锁定，LLM 不可修改（设计 §4.6）。
\documentclass[11pt,a4paper]{article}

\usepackage{ctex}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{hyperref}
\hypersetup{hidelinks}
\usepackage{xcolor}
\usepackage[margin=2.5cm]{geometry}
%% gbt7714 宏包缺失时降级为 unsrt：编译成功优先于书目风格（draft-first）。
\IfFileExists{gbt7714.sty}{\usepackage[super]{gbt7714}}{}

%% 浮动体排版参数（核心 LaTeX，不引入任何宏包——texd 运行时无外网，
%% Tectonic 的包缓存只在镜像构建期预热，新增 \usepackage 会直接编译失败）。
%%
%% 默认值非常保守：一页最多 2 个顶部浮动体、正文至少要占 20%，稍微放不下
%% LaTeX 就把图推进延迟队列，队列只在 \clearpage / 文末冲刷——这正是
%% 「图片全堆在论文最后」的成因。下面把配额放宽到排版上仍然合理的范围。
\setcounter{topnumber}{3}
\setcounter{bottomnumber}{2}
\setcounter{totalnumber}{5}
\renewcommand{\topfraction}{0.9}
\renewcommand{\bottomfraction}{0.7}
\renewcommand{\textfraction}{0.1}
\renewcommand{\floatpagefraction}{0.75}

\providecommand{\todo}[1]{\textcolor{red}{\textbf{[TODO: #1]}}}

\title{ {{- title -}} }
{% if author_block %}\author{ {{- author_block -}} }{% else %}\author{}{% endif %}
\date{}

\begin{document}
\maketitle

{% if abstract %}\begin{abstract}
{{ abstract }}
\end{abstract}{% endif %}
{% if keywords %}

\noindent\textbf{关键词：} {{ keywords }}
{% endif %}

{{ body }}

\clearpage

{% if has_bibliography %}%% gbt7714 宏包自己会发 \bibliographystyle{gbt7714-numerical}；这里再发一次
%% 就是 .aux 里的第二个 \bibstyle，BibTeX 会报
%% 「Illegal, another \bibstyle command」。所以只在宏包缺失时才补 unsrt。
\IfFileExists{gbt7714.sty}{}{\bibliographystyle{unsrt}}
\bibliography{refs}
{% endif %}
\end{document}
