# Research Agent v1 验收（2026-09-23）

实现说明见 [架构与使用边界](../research-agent-architecture.md)。本次新增的是受控描述统计、图表和确认后追加文稿的完整流程，不宣称具备任意数据科学代码执行、显著性检验或科研质量提升。

## 自动验证

- 后端包、worker、texd、visuald：1287 passed / 18 skipped。未配置的外部集成跳过项不计入通过数。
- API 全量：205 passed，使用本次创建的独立 PostgreSQL 测试容器，不使用生产数据库作为测试库。
- 后续研究接口定向回归：5 passed，包含真实 PostgreSQL graph checkpoint 关闭连接后恢复。
- 后续研究计算、沙箱、Pi、软核验及工具模型接口：51 passed；另一次工具/沙箱定向组合 4 passed。以上与完整测试有重叠，不相加为独立测试总数。
- 既有前端：144 passed；新增研究面板与素材中心定向：6 passed（其中 2 项新增），覆盖明确确认与文稿冲突提示。
- TypeScript 检查、Next.js 生产构建、Ruff、git diff --check 通过。
- Playwright 桌面 1280px / 手机 390px：2 passed，确认前不发送修改，确认后显示新版本链接，无页面水平溢出。这两项使用受控 API 响应，不等同于真实模型验收。
- Alembic 从空测试库升级到 0035，通过 `alembic check`，无新增漂移。
- Worker 发布镜像在非 root、只读文件系统、禁网络、cap-drop ALL 容器中完成计算与 PNG 绘制；Node v24.21.0 可启动。
- 沙箱负例：其他文件读取、socket 创建、只读打开后截断文件均被拒绝；原文件保持完整。

测试修复记录：完整 API 与 worker 不能混放到同一次 pytest 采集，否则既有同名 conftest 导入冲突；已按仓库测试目录分开运行。首次本地 Next 构建与 dev 共享 .next 发生冲突，停止临时 dev 后生产构建通过。首次镜像构建的 Debian HTTP 源下载缓慢，改用 HTTPS 和有限超时后构建通过。

## 可复现计算与恢复边界

固定样例检验分组均值、样本标准差、单样本不可计算、缺失值计数、非有限值拒绝、多工作表澄清、公式拒绝。派生素材由确定性计算器写入，包含输入哈希与计算参数。

Pi 使用真实 Node/Pi Agent Core 进程加模型替身验证原生 tool-call 循环；生产模型验证结果另列。图检查点与业务产物分别持久化，恢复复用稳定产物 ID。数据库提交具备幂等性；外部模型请求不承诺 exactly-once。

## 发布与公网验收

首次发布 `paperforge-deploy-20260923-232824` 通过 Funnel 和公开 `/login` 字节一致性检查，旧部署保留。公网合成项目已完成确定性统计、人工输入恢复、PNG/复现包下载、确认生成新版本及重复确认幂等验证。

首次真实 Pi 尝试在模型调用前暴露了 provider 工厂导入路径错误；没有把仅 Node 层的替身测试描述成完整桥接通过。修复导入后新增 Python 网关 + 真实 Pi 进程 + 模型替身的完整测试，确认工具调用与两次费用账本记录。另修复非空引用白名单的字典处理，并新增覆盖；失败状态可在结果接口显示。修复后运行时 4 项、研究接口 5 项回归通过。

最终发布 `paperforge-deploy-20260923-233629`，本地端口 3007，所有服务健康。`./scripts/dev restart` 完成 Funnel 目标核对及公网 `/login` 与新部署逐字节比较，SHA-256 为 `6376bf431d312788ec821f1dcc67af15cf2e34947c89a36674fbf7f1e046a28c`。公网地址：https://paperforge-linux.tail53ab99.ts.net 。此前部署均保留，未停止持有在途任务的 worker。

真实公网 Pi 验收从失败任务的检查点恢复，使用生产配置模型 `glm-5.3-flash` 完成 5 次模型调用，输入 4251 tokens、输出 1029 tokens；账本无未知用量、无未定价调用、无调用错误。按配置单价估算费用为 0.003141（非供应商结算账单）。生成的两组均值分别为 2、3，与确定性样例一致。该结果证明本次桥接及工具链可用，不构成模型科研质量评估。

实际公网浏览器验收使用已登录的合成账号，在 1280px 和 390px 两种宽度检查素材中心：图表加载、确认后的新版本链接、无横向溢出，且无浏览器页面异常。该检查独立于前述模拟 API 的 Playwright 用例。

验收完成后已归档本次合成项目、禁用合成账号、撤销其全部会话并移除本地会话令牌；仅移除本次创建的独立测试数据库容器。截图、统计图、复现包及不含凭据的摘要保留于本机忽略目录 `.paperforge/research-agent-20260923/`。
